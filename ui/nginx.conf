server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — everything that isn't a real file serves index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
